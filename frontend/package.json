{
  "name": "notes-app-frontend",
  "version": "1.0.0",
  "description": "Frontend for Notes Application",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 8080",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 8080"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^4.4.9"
  }
}
